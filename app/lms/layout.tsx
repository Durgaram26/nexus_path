import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'NexusPath LMS - Learning Management System',
  description: 'Advanced Learning Management System with Adaptive Quiz Technology',
  keywords: 'LMS, Learning Management, Adaptive Quiz, Education, Online Learning'};

export default function LMSLayout({
  children}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  );
}
